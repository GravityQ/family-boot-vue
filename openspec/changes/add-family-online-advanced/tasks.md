## 0. Dependency(依赖)
- [ ] 0.1 确认 `add-family-online-mvp` 规格已冻结（验证：记录依赖点与接口/实体契约版本）

## 1. Search(搜索能力)
- [ ] 1.1 实现家族搜索分页：支持按 姓氏/地区/字辈 等组合（验证：单测覆盖组合条件与分页排序）
- [ ] 1.2 未加入家族的搜索结果脱敏：仅返回概况字段（验证：单测覆盖“未加入不可返回树谱/成员详情入口”）

## 2. Notifications(消息与通知)
- [ ] 2.1 管理员待办通知：当审核记录进入 PENDING 时触发通知（验证：单测覆盖事件触发与幂等）
- [ ] 2.2 申请结果通知：审核通过/驳回后通知申请人（验证：单测覆盖“驳回原因透传但不泄露多余字段”）
- [ ] 2.3 通知渠道适配：复用平台现有站内信/订阅消息能力（验证：设计决策记录在 `design.md`）

## 3. Tree loading strategy(树谱加载策略)
- [ ] 3.1 默认 5 代视图：首次只加载 5 代数据（验证：单测覆盖“代数裁剪”）
- [ ] 3.2 边界节点展开：按需加载子代（验证：单测覆盖“按父节点/家庭单元加载子女”）
- [ ] 3.3 大家族性能保护：>500 人家族启用懒加载策略（验证：单测覆盖阈值分支；压测留到实现阶段）

## 4. Cross-family association(跨家族关联)
- [ ] 4.1 配偶绑定：录入配偶时支持按 FamilyID/MemberID 搜索并绑定（验证：单测覆盖“存在/不存在/无权限可见”）
- [ ] 4.2 隐私限制：对方家族为 PRIVATE/PROTECTED 时按策略限制跳转或隐藏细节（验证：单测覆盖“不可泄露存在性/详情”）

## 5. Validation(校验)
- [ ] 5.1 运行 `openspec validate add-family-online-advanced --strict`（验证：严格模式通过）


